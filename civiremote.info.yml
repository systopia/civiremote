name: CiviRemote
type: module
description: 'Core and Tools module for CiviRemote'
package: CiviCRM
core: 8.x
configure: civiremote.config_form
dependencies:
  - cmrf_core

'interface translation project': civiremote
'interface translation server pattern': https://github.com/systopia/civiremote/tree/1.x/translations/%language.po

project: civiremote
version: 1.x-dev
